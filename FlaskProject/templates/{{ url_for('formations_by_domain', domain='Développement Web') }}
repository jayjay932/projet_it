from flask import Flask, render_template, url_for

app = Flask(__name__)

@app.route('/formations/<domain>')
def formations_by_domain(domain):
    return f"Formations for domain: {domain}"

if __name__ == '__main__':
    app.run(debug=True)